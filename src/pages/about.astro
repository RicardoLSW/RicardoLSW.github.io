---
import BaseLayout from '../layouts/BaseLayout.astro';
import { site } from '../lib/site';
---

<BaseLayout title="关于" description={`关于 ${site.author} 和这本旅行影像日志。`}>
  <div class="page-shell">
    <header class="page-header">
      <div><p class="section-kicker" lang="en">About</p><h1>关于这里</h1></div>
      <p>这里持续记录 Ricardo 旅途中看见的风景与抵达的城市。</p>
    </header>
    <div class="about-grid">
      <div class="prose">
        <h2>照片在前，文字随后</h2>
        <p>这是一本以旅行照片和沿途笔记为核心的影像日志。首页、归档与地图会随着新的旅行内容自动更新。</p>
        <p>每次旅行是一篇独立的 Markdown 或 MDX 内容：地点、日期、城市级坐标、封面、标签和相册都写在同一份内容文件中。</p>
        <h2>位置与图片</h2>
        <p>地图只使用城市级坐标，不公开具体拍摄位置。旅行图片在构建时生成适合网页浏览的 AVIF/WebP 版本，原始照片不会被页面默认全部下载。</p>
      </div>
      <aside class="about-profile">
        <img src="/assets/images/bio-photo.jpg" alt="Ricardo 的头像" width="180" height="180" />
        <p class="section-kicker" lang="en">The author</p>
        <h2>{site.author}</h2>
        <p>{site.bio}</p>
        <ul class="tag-list" aria-label="社交链接">{site.social.map((item) => <li><a href={item.href} target="_blank" rel="me noopener">{item.label}</a></li>)}</ul>
      </aside>
    </div>
  </div>
</BaseLayout>
